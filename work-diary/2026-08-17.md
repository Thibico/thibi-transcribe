# 2026-08-17 — Phase 9: cancellation, and a guarantee kept out of a listener

Executed against [phase 9](../plans/phase-09-queue-and-worker.md), on branch `phase-9/cancel`.
`main` has PRs #39–#42; the merge order for the stacked pair went wrong and #41 was the repair —
see the note at the end.

---

## §10, with its central mechanism inverted

**[`78d1885`](https://github.com/Thibico/thibi-transcribe/commit/78d1885) — Make a cancel reach
the step that is already running**

The rule §10 opens with is that *a cancel that only stops the next step is not a cancel*. The
expensive work is the step already in flight — a chunk mid-request, a forty-minute diarization —
and reaching it is the whole problem. The plan solves it with a `LISTEN run_cancel` channel that
every worker subscribes to.

The notify is sent, because the UI will want it and because adding a subscriber later should be
a subscriber and not also a producer. But nothing subscribes to it, and the mechanism that
actually reaches a running handler is the heartbeat's existing statement with one extra join:
`returning (r.cancel_requested_at is not null)`.

**The reason is failure mode, not latency.** A `LISTEN` needs a dedicated non-pooled client held
open for the life of the process, and when it dies nothing throws — cancellation silently stops
working until somebody notices a run that will not die. This phase has an open investigation
into exactly that shape: a poll chain that went quiet for two hours with nothing logged. Putting
a *guarantee* behind a second subsystem with the same failure mode would have been repeating the
mistake while still reading the report on it.

The heartbeat is the opposite shape. It already runs every fifteen seconds for every running
step, already round-trips to Postgres, already aborts the handler when it dislikes what it
finds. The state lives in the row rather than in a message that was or was not delivered, so a
missed check is retried fifteen seconds later, forever. The price is up to fifteen seconds of
latency — and **a cancel that arrives late is a delay where a cancel that never arrives is a
bug**. The listener is still worth building, as an accelerator over that guarantee rather than
as the guarantee. Amendment 102.

Migration `0005` adds `runs.cancel_requested_by`, which §10 has been writing since phase 1
without the column existing. `text` rather than a `users` reference, on two counts: the CLI
cancels with nobody signed in, and a run's audit trail has to outlive the account that triggered
it — a cascade would delete precisely the record someone is asking about.

`requestCancel` moved out of `batch-persist.ts`, where a second and thinner version had been
living: it wrote the timestamp and nothing else — no requester, no event, no notify. Two
functions of the same name doing different amounts of the same job is the drift this repo has
already paid for twice, so that one is now a re-export of the real one. It is idempotent by
predicate rather than by check, returns whether it actually acted, and deliberately does not
reconcile: it is called from places with no doorbell on the context, so the caller reconciles
when it can and the thirty-second tick does it otherwise.

## A busy sidecar is not a broken one

Running the full suite while a worker was mid-diarization turned `pyannote.contract.test.ts`
red. Its `beforeAll` submits a task, my own run held the sidecar's only slot, and
`DiarizerBusyError` escaped as a suite failure.

That is wrong twice over: nothing is broken, and the message names a busy queue rather than a
bug. "Somebody else is using the machine" belongs with "the sidecar is not running", which this
file already skips for. Its readiness probe now checks `slots.busy` alongside the model state.

It only became reachable this week, which is why it survived until now: before the `diarize`
handler existed, nothing in the system but that test ever submitted a task, so the slot was
never contended.

## The stall hunt: nothing caught, and a number worth having

A 16 m 37 s recording was put through deliberately to try to reproduce the two-hour poll stall,
with the new overdue-poll warning armed. It finished clean: **95 polls at a steady sixteen-second
cadence**, no `overdue` warning, no recovery tick repairing anything — against the 23 polls at
which the original broke. Both long-async shapes ran in the same run, batch ASR finishing in 5
polls and 390 s. All eleven steps `done`, 295 turns attributed across 35 segments, two speakers,
$0.04985.

**That is not a clean bill of health.** The stall has been seen once and not reproduced, and a
hunt that catches nothing narrows nothing by itself. What it does rule out is a defect that fires
reliably on every long poll chain — whatever this was, it is intermittent.

What the run *did* settle is a number the project has been quoting badly. Diarization on this box
runs at **0.669× realtime** — 16 m 37 s of audio in about 25 minutes of CPU. That is the first
diarization figure measured on a clip long enough to clear amendment 56, and it **confirms** S6's
0.6× rather than contradicting it: pyannote 4.0.7 is not meaningfully slower than the 3.1 S6
measured. Yesterday's worry that it had become four times slower came from two probes that were
too short and, in one case, taken while a test suite competed for the machine. A one-hour
interview is about an hour and a half of diarization, which is what the GPU-tier question
(open question 3) actually needs to be answered against.

With the slot free again, the pyannote contract suite was re-run and all six passed — so the six
skipped in the commit above are now accounted for rather than left as a green-looking gap.

---

*Merge-order note.* PRs #39 and #40 were stacked, and merging #39 first left #40 targeting a
base that had already been merged, so its commits never reached `main`. GitHub only retargets a
stacked PR when its base branch is deleted on merge. #41 carried them across. Nothing was lost,
and the lesson is in the handoff: delete the base branch on merge, or retarget the child first.


---

# Third sitting — the rate bucket, and a test eating the machine

**[`6b724bd`](https://github.com/Thibico/thibi-transcribe/commit/6b724bd) — Build the outbound
rate bucket, and stop one test starving the suite**

`queue/rate-bucket.ts` is the last of §6's three concurrency layers, and the only one that can
see what other *processes* are doing — because its state is a row rather than a variable. Layer 1
is which container subscribes to what; layer 2 is `batchSize`; neither can notice that ten
containers each politely respecting a cap of eight is eighty concurrent requests against one
Google project.

Two things about it look wrong and are not. **The debit is unconditional and the balance goes
negative**: a taker with no tokens takes them anyway and is told how long to wait for the deficit
it created. A conditional debit would refuse the caller, who would poll, so every worker would
spin against a bucket refusing them all and the unluckiest would starve; overdrawing makes them
queue behind *each other*, fair and roughly FIFO. And **an unconfigured provider is
unthrottled**, because the other default makes the throttle something you must remember to
configure before anything works, and the symptom of forgetting is a system that looks broken on
first run.

Wired into `asr.chunk`: short waits sleep, long ones return `no_slot`, because a worker asleep on
a bucket holds a lease and does nothing. A provider 429 penalty-debits half the capacity and is
then rethrown — that is how one chunk's rejection slows its seven siblings with no cross-process
messaging, and it differs from the local-sidecar case deliberately: contention for our own slot is
scheduling, being over someone else's quota is a fault worth a visible attempt.

## Then the suite started failing four tests at a time

In a *different* set each run: `constraints`, `persist`, `reconcile`, even the CPU-bound
`hungarian`. Nothing in common except being expensive.

The cause is one the handoff has carried for several sittings as an occasional annoyance:
`pyannote.contract.test.ts` performs a **real** diarization — about 40 seconds of CPU inside
Docker's Linux VM for an eleven-second clip — concurrently with eighty other files, and it starves
the Postgres suites into hook timeouts. Nineteen new database-backed tests across the last two
sittings tipped a marginal situation over.

**My first attempt made it worse, and that is the diagnostic.** I raised its internal deadline
from 150 s to 420 s, reasoning that the timeout was too tight for a loaded machine. The failures
spread. The deadline was never too short — the test was too expensive, and lengthening it simply
let it consume more of the machine other suites needed. Reverted.

Vitest offers no way to run one file alone, so it is out of the default pass behind
`pnpm test:contract`, gated on an env var rather than split into its own project — a project would
still be collected by a bare `vitest run`.

**The cost is real and is not glossed.** The one test that checks our hard-coded wire shapes
against what `schemas.py` actually emits no longer runs by default, so the two halves can drift in
silence. It belongs in the definition of done for any change to the sidecar or `pyannote.ts`, and
the handoff and the config both say so. There is precedent: the sidecar's own Python suite is
outside `pnpm test` for the same reason.

## And three flaky tests of my own

Written the same sitting, and worth naming as a class. They asserted wall-clock windows — "the
wait is between 1.5 s and 3 s" — against a bucket refilling once a second, so they passed alone
and failed under load. Rewritten to assert the arithmetic against the balance the row actually
holds, with refill rates low enough that only the deficit moves. **A test whose outcome depends
on how fast the suite happens to be running is measuring the machine.** Amendment 104's
corollary.

`pnpm test`: 1301 across 84 files, nothing skipped, three consecutive clean runs.
`pnpm test:contract`: 6 in 58 s.

## Not code: the machine ran out of disk

Mid-sitting the data volume hit 100% — 607 MiB free of 466 GiB — Docker Desktop refused to start,
and every Postgres- and MinIO-backed suite skipped itself. The handoff's 2026-08-13 entry covers
the recovery and needed two additions: at 100% Docker refuses to *start* rather than starting and
hanging, and freeing space is still not sufficient — `docker ps` then hangs, and the documented
quit/`pkill`/relaunch sequence is required.

Recovered to 39 GiB: about 13 GiB that macOS released as purgeable once pressure eased, 13.6 GB
from `npm cache verify` (npm's own GC on a 20 GB cache), 8.7 GB of Android emulators and system
images, and 4.1 GB of application caches. **The caches were never the cause** — `~/Library` is
175 GB, of which Application Support is 82 GB and a mirrored Google Drive is 52 GB. The lesson for
the note: survey the volume, do not reach for the caches.
