# Work diary

One file per working day, `YYYY-MM-DD.md`, covering every commit made that day.

Each entry answers *what was worked on, what materially changed, and why* — and links the
commit to the plan document it executes against, so a phase plan and the code that
implemented it can be read together months later. Commit messages carry the reasoning;
this is the day-level narrative that connects them and records what the plans got wrong.

Written after the commits, not instead of them. If a day produced no commits it gets no
file.

| Date | Covers |
|---|---|
| [2026-08-09](./2026-08-09.md) | Repo created · 16 plan documents · Phase 0 spikes · Phase 0 monorepo, language registry, CLI and live provider probe |
| [2026-08-10](./2026-08-10.md) | Phase 1 engine: core primitives, storage port, database schema, audio stages, seam merge, Google provider, pipeline and `thibi transcribe` · Phase 2 batch: the staging port, `batchRecognize`, routing corrected against spike S3, and the 192 kHz normalize bug |
| [2026-08-11](./2026-08-11.md) | Phase 8 merged and the session-handoff convention · Phase 4a: the shared Whisper HTTP transport, OpenAI and Groq, script integrity, `chooseProvider`, and four defects that only running it found · Phase 3 begun: spike S7 kills the hosted-diarizer alternative, then the reconciler, the Hungarian matcher, the speaker schema and the sidecar · Phase 3's seam and surface: `persist.ts`, the diarize stage, `thibi speakers`, `thibi diarize` — and a rename-survives demo that ran against a stand-in and, as 2026-08-12 found, would not have passed against the real thing |
| [2026-08-12](./2026-08-12.md) | The presigned-URL host gap, and the identity matching it exposed as never having run through any path a user has — plus the plan's own demo, which passed while proving nothing · `thibi transcribe --job <id>` and the guard that makes it safe · the sidecar contract test, run against the real model rather than the canned pipeline the plan asked for · Phase 5's metrics layer: CER, WER, chrF2, the scoring normalizer and the seeded bootstrap, frozen against real `jiwer` 4.0.0 / `sacrebleu` 2.6.0 numbers — and six amendments, including a grapheme cluster that is not a Burmese syllable |
| [2026-08-13](./2026-08-13.md) | Hosted diarization reopened as an evaluation — not a promise — with the criteria a candidate has to clear, data residency first among them · two CLI defects, one of which was never real · the runner and the first measured CER · publishing it: the runlog, `tiers.json`, the dated report, and a `--budget-usd` check that was never a budget · the first n=30 sweep, the wav reader its regularity exposed, and `my-MM` losing `verified` to its own measurement · `tiers.json` v2, accumulating the evidence and deriving the claim · Yoruba's error found to be 75% diacritics, tar order cleared of bias, and the baseline re-measured at n=100 |
| [2026-08-14](./2026-08-14.md) | The LLM evals delegated to a cloud agent, which stalled having written nothing; then built locally, measured once, and parked. Phase 9 begins: the run DAG, its planner and its reconciler — and five defects in the phase plan, one of which made a `partial` run unreachable. Then the pg-boss adapter, the execution path, one shared composition root, and a worker that boots — which immediately found a sixth |
| [2026-08-15](./2026-08-15.md) | `main` unblocked — the region grep that had been failing since PR #37, and the shared home that stops a third app repeating it · Phase 9's five handlers, `thibi runs start`, and the seam merge deciding where persistence lives · then running it: an empty transcript reported as success, the wildcard-over-zero-shards defect behind it, and a sixteen-minute Burmese transcript · `kill -9` mid-run, with two already-paid-for chunks not paid for twice · **second sitting**: the batch ASR path, `awaiting_external` found to be a state no mechanism could send, and a live `batchRecognize` across a `kill -9` that exposed a run cost 5.3× too high |
| [2026-08-16](./2026-08-16.md) | Phase 9's diarize trio — the third long-async pair, and the third time §7 assumed one step where its own tables list two · §6's advisory-lock slots shown to contradict §7, and §7 wins · the hole between two correct fixes, where a null `poll_after` left a step rung by nothing · three live runs: the no-sidecar skip path by accident, a poll chain that stalled for two hours and lost a diarization that was 64% done, and a clean end-to-end trio on the fixed build · **second sitting**: pg-boss ruled out as the stall's cause by two tests, one of which the class comment had been citing for days without it existing · an overdue-poll warning, because the incident's real cost was that nothing said anything |
| [2026-08-17](./2026-08-17.md) | Phase 9's cancellation, with §10's `LISTEN` channel demoted to an accelerator and the heartbeat made the guarantee — because a dropped listener fails silently, which is the shape already under investigation · `cancel_requested_by`, three phases after §10 started writing it · a busy sidecar reported as a test failure rather than a skip · the stall hunt, running and so far catching nothing |
