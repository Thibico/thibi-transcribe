// The language registry: static JSON -> generated frozen TS -> DB overrides merged at
// resolve time. Never touches Postgres; overrides arrive as an argument.
//
// Placeholder: this package exists now so the dependency graph — and the ESLint
// rule that enforces its direction — is real from the first commit.

export {};
